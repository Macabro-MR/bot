const fs = require('fs');
const chalk = require('chalk');

global.prefix = ["!"] // prefixo do bot 

global.nomedobot = ["TheCybers - MD"] // nome do bot 
global.nickdono = ["</> MrRoots </>"] // nome do dono 
global.browser = ["Safari"] // navegador, pode ser qualquer navegador. se você souber o nome, claro.
global.versionbrowser = ["3.0"] // versão do navegador. 
global.mychannel = ["https://youtube.com/@ManiimRoots"] // link do seu canal. 

global.dono = ["554891089432"]